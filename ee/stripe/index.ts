export const stripe = null;

export const stripeInstance = (_account: boolean = false): any => {
  return null;
};

export async function cancelSubscription(
  _customer?: string,
  _isOldAccount: boolean = false,
) {
  return null;
}
